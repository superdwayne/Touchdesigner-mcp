console.log("TouchDesigner MCP connection active");
console.log("Your TouchDesigner server should be running at http://localhost:8051");
// This process will stay alive until terminated
setInterval(() => {}, 1000);